package com.example.visionwidget.ui

/**
 * Every screen holds its content at this fraction of the width and centres it, so
 * headers, labels and cards share the same edges from one tab to the next.
 */
const val ContentWidthFraction = 0.9f
